# version 1
edi.segment(UNB) {
    segment(UNH) {
     // some data
    }
    segment(UNH) {
     // some data
    }
}

# version 2
edi.segment(UNB) {
	segment(UNG) {
		ds("UNOB": 1).ds("gcms003": "ZZ").ds(101013: 1129).de(1013115727000).de().de("CLSVAL")
		segment(UNH) {
         // some data
		}
		segment(UNH) {
         // some data
		}
	}
}
